#include "main.h"
#include <stdio.h>
#include <stdio.h>
#include <string.h>
/**
 * _strpbrk - memory set
 *
 * @s : s
 * @accept: b
 * Return: Nothing.
 */

unsigned int _strpbrk(char *s, char *accept)
{
	return (strpbrk(s, accept));
}
