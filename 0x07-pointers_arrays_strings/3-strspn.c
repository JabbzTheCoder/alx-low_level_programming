#include "main.h"
#include <stdio.h>
#include <stdio.h>
#include <string.h>
/**
 * _strspn - memory set
 *
 * @s : s
 * @accept: b
 * Return: Nothing.
 */

unsigned int _strspn(char *s, char *accept)
{
	return (strspn(s, accept));
}
